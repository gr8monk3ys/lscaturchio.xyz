---
target: * (whole site)
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 4
p1_count: 6
timestamp: 2026-09-07T18-04-03Z
slug: src-app
---
Method: dual-agent (A: design review · B: detector + browser overlay). Cycle 3, after the design-system pass, the architecture refactor (#207) and the review fixes (#206).

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 3 | `navbar-controls-gate.tsx:31-44` renders an `aria-hidden`, non-interactive Search/theme placeholder for up to 1200ms |
| 2 | Match System / Real World | 3 | `/projects` reports "Featured: 3 · Categories: 4" — internal taxonomy shown to a visitor |
| 3 | User Control and Freedom | 2 | `BackButton` rewritten and dead (no essay passes `previousPathname`); `/projects` view mode is `useState` while sort/category are URL params |
| 4 | Consistency and Standards | 1 | 5 hand-rolled stat rows, 13 counterfeit wall labels, 3 colour systems, 2 inset shadows against a no-shadow doctrine, home stacks max-w-6xl over max-w-7xl |
| 5 | Error Prevention | 2 | `Hero.tsx:55` ask field `required` with no message; `/projects` filters reach empty with no empty state |
| 6 | Recognition Rather Than Recall | 3 | Ledger numerals, breadcrumbs, `EssayContentsInline` disclosure all good |
| 7 | Flexibility and Efficiency | 3 | ⌘K palette, per-essay audio, RSS, contextual chat via `?contextSlug` |
| 8 | Aesthetic and Minimalist Design | 2 | `/projects` = 13 controls + 3 stat tiles + 18 items + a sales card; `/blog` ends in a dashboard; `/stats` ranks the same data twice |
| 9 | Error Recovery | 2 | `text-red-600` bypasses the Error token in 7 files |
| 10 | Help and Documentation | 3 | `/colophon`, `/uses`, `/api-docs`, `/privacy-policy` thorough and in-voice |
| **Total** | | **24/40** | **Acceptable** (was 28) |

## Design Specificity Verdict

~60% of the surface is authored for this product; ~40% is a portfolio template with the colours changed. The split is geographic, not random: the design-system pass stopped at a clean repo boundary and never crossed it. Authored: `src/components/home/*`, `BlogLayout.tsx`, `/garden`, `/music`, `/chat`, `ledger-section.tsx`. Category-interchangeable: the whole `src/components/projects/` subtree, `/professional`, `/changelog`, `about/Music.tsx`, `blog-stats.tsx`, `contact-cta.tsx`, `src/components/stats/*`, `contribution-graph.tsx`, `PhotosGrid.tsx`.

Two hard measurements: `label-mono` is the signature utility and 13 places hand-roll a counterfeit in the wrong typeface (8 in `product-sections.tsx` alone); there are 5 independent stat-row implementations.

Detector: 20 CLI findings, all judged non-actionable (14 known-FP sources, 3 documented in DESIGN.md, 1 dead `animate-bounce`). Live in-page detector injected successfully over 9 routes x 2 widths. Real measurable hits: `line-length` (/movies 27 paragraphs at ~110ch), `tight-leading` (/work-with-me FAQ at 1.10), `nested-cards` x15.

## Priority Issues

- [P0] `books/page.tsx:17` says "the three books I gave full marks"; the CSV has SIX. Verified independently. The page body four lines away renders `spellCount(perfectScores.length)` = "Six books", and `BooksList.tsx:132-133` carries a comment explaining this exact bug being fixed once already. Live falsehood in an indexed meta description on a site positioned on verified truth.
- [P0] Delete the testimonial scaffold. `testimonials-section.tsx` defaults to "What People Say" / "Kind words from colleagues and clients I have worked with", uses a banned `lg:grid-cols-3` grid, `neu-card`, and the `whileInView` stranding pattern. PRODUCT.md names testimonials an absence that must not be filled; the code makes filling it one boolean away, pre-styled.
- [P0] Delete `blog-stats.tsx`. Independently corroborated: A flags 6 rule violations; B measured its inset shadow and nested cards in the live DOM. "Browse at a Glance" / "N/A" empty state.
- [P0] Convert `product-sections.tsx` to the ledger vocabulary. Three-column feature tiles at :193-224 with orange/yellow icons, `bg-primary/5` as area, 6 counterfeit labels.
- [P1] The drop cap DESIGN.md names as a signature has never rendered on any of 83 essays. `globals.css:644` selects `.prose-gallery > div > p` but MDX lands as a DIRECT child (verified in prerendered HTML). The one-line fix misfires: 81 of 83 essays open with `##`, so `> p:first-of-type` would float a 3.4em initial under a section heading.
- [P1] Mobile touch targets. B measured 5 share buttons at 14x14 on an essay, 14 `/garden` index links at 15px tall, footer links 17-20px tall. Under WCAG 2.5.8's 24px floor, well under the 44px comfort target.
- [P1] `/movies` contrast 2.42:1 (`rgb(157,162,169)` on paper, 11.52px, x3) and 3.22:1 x1. Fails AA.
- [P1] `/work-with-me`: 3 primary CTAs and 11 consecutive rule-of-three lists. writing-style.md §2 names the triple cascade the second-worst tell in the corpus.
- [P1] `changelog/page.tsx:75-110` second banned tile grid; `contribution-graph.tsx` third colour system + `rgba(0,0,0,0.1)` hairline on warm paper.
- [P1] `/professional` fully off-system; timeline's sourced figures render with no visible provenance (reconciliation exists only in a code comment).
- [P2] Flat Paper Rule broken in 14 files; `about/Music.tsx` album blurbs are LLM-written; `services.ts` 48 filler bullets; mono as heading voice in 6 components.
- [P2] `/blog/strikes-work` renders `h2 "Ask about this post"` TWICE. `portrait.webp` 1200x1244 rendered at 32x32 on `/chat` (37x oversize).

## Where A and B disagree

A flagged 7 components as `whileInView` stranding-capable. B measured zero elements at `opacity: 0` across 18 route/width combos. The bug is latent in source, not currently reproducing at load. Treat as a code-hygiene item, not a live defect.

B retracted three of its own findings after visual verification (nav focus ring delivered by box-shadow not outline; skip link sampled mid-transition; `/projects` white-on-image contrast, where both B and the live detector share an `<img>` blind spot). Zero hydration errors, zero horizontal overflow, one h1 per route, no heading skips.

## Persona Red Flags

- Jordan: "New here? Start with these" is section 04, at the BOTTOM of the home page, after the hero, currently-strip, three essay groups, three case studies and eight favourite rows.
- Sam: header has no search/theme in the a11y tree for ~1.2s; `blog-sidebar.tsx:200` uses literal neutral grey, the one colour DESIGN.md forbids for rules.
- Casey: 14x14 share buttons; Ask panel relocates to AFTER eight end-matter blocks on mobile.
- Mid-essay reader (primary persona): best-in-class arrival, then eight end-matter blocks; clicks Books and reads "three" over a body saying "Six".

## Minor

- `Section.tsx:65-87` renders section dividers as centred half-width dashes while `gallery-rule` exists for the purpose.
- `changelog/page.tsx:151` closes `<section>` before `<ChangelogTimeline />` renders.
- `garden/page.tsx:38` "Photographs" vs `photos/page.tsx` "Photography".
- Dead: `back-button.tsx`, `navbar.tsx:15` `ACCENT_HREF=""`, `services/page.tsx:6` metadata on a redirect, DESIGN.md:246-250 documents `--neu-shadow-*` tokens that no longer exist.
- `footer-section.tsx:105` "A garden, not a homepage" is the best six words on the site, set in 0.72rem mono in the legal row.

## Questions to Consider

- Why is `/projects` a database? The homepage already knows how to present projects as a numbered ledger.
- If the essay reader is the primary user, why is the end of an essay the most crowded screen on the site?
- Is the problem that the site lacks honesty guards, or that the guards live in components while the lies live in `metadata` objects? The corpus has a drift check. The counts do not.
