---
target: * (whole site)
total_score: 23
max_score: 40
na_heuristics: 
p0_count: 3
p1_count: 4
timestamp: 2026-09-09T06-52-53Z
slug: src-app
---
Method: dual-agent (A: design review · B: detector + browser). Cycle 4, measured on `review/integration` (all four open PRs merged locally: #210 copy pass, #211 widths + essay, #212 drawer/rail/projects, on top of merged #208 and #209).

## Design Health Score

| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Ask trigger has no accessible name below lg; drawer has no aria-live, answers arrive silently |
| 2 | Match System / Real World | 2 | /changelog contradicts itself about its own provenance, 40 lines apart, after the copy pass |
| 3 | User Control and Freedom | 2 | Drawer is modal on phones with no focus trap, no aria-modal, no backdrop dismiss |
| 4 | Consistency and Standards | 1 | Three ask affordances with three behaviours; four container widths where DESIGN.md specifies three; two neutral palettes (paper + stone) |
| 5 | Error Prevention | 3 | count-drift guard is real, but reads only 4 nouns in .ts/.tsx — the 83 MDX essays are outside it |
| 6 | Recognition Rather Than Recall | 3 | EssayContentsInline, PageHead kickers, breadcrumbs all consistent |
| 7 | Flexibility and Efficiency | 3 | Drawer from any route, seeded questions land in the composer rather than auto-sending |
| 8 | Aesthetic and Minimalist Design | 2 | /changelog tile grid and product-sections card stack unchanged; /stats still ranks the same data twice |
| 9 | Error Recovery | 3 | failedQuery preserved for exact retry; honest empty states |
| 10 | Help and Documentation | 2 | The new drawer ships with no affordance explaining what it is beyond an unlabeled sparkle |
| **Total** | | **23/40** | **Down from 24, and from 28 at cycle 3** |

## The finding that matters

Three times in this cycle I wrote a comment stating a rule and then shipped code violating it in the same breath:

1. `ask-drawer.tsx:76` — "No shadow: the Two Sheets Rule spends both of its elevated objects already" — then mounts `ChatMessageList`, which carries `shadow-md` at line 40.
2. `product-sections.tsx:309` — "The Flat Paper Rule forbids a lift on hover, and group-hover:scale-105 on an image is one" — while `ProjectGallery.tsx:207`, `BlogCard.tsx:45` and `related-posts.tsx:81` all ship `group-hover:scale-[1.03]`. The live detector counts 18 instances on /projects alone.
3. The new essay says the drift check "greps the source for a hand-written count of anything the site owns". It greps four nouns in .ts/.tsx under four roots.

Documented intention, unverified. Same failure I spent the cycle flagging in older code.

## What deterministically improved

- `max-w-400` (1600px) and every `max-w-7xl` eliminated; content columns are exactly 1152px on 11 of 12 routes.
- Zero horizontal overflow on 24 route/width combinations.
- Zero elements stranded at opacity 0 after scrolling — the whileInView bug is genuinely gone.
- Shadows: exactly the two sanctioned objects plus the known navbar pill inset.
- No React hydration errors, no warnings, silent console on 10 of 12 routes.
- Drawer geometry verified: shell padding and header inset both 0→384px, focus enters the composer, Escape closes and restores focus, box-shadow none, 1px hairline border, mobile keeps shell padding at 0 and locks body scroll.

## Priority Issues

- [P0] `ask-drawer-trigger.tsx:34` — below lg the label is `hidden lg:inline` (display:none removes it from the accessible name), the sr-only sibling is the empty string, and the icon is aria-hidden. The only persistent route to the headline feature announces as an unnamed "button".
- [P0] The drawer crushes the essay it exists to keep in view. At 1280px the prose column falls 672→~492px because `xl:grid-cols-[260px_1fr]` keys off viewport width and cannot know the drawer opened. Below ~1536px it should overlay, or the rail should collapse on `[data-ask-open]`.
- [P0] No focus trap and no `inert` background. Forward Tab from the composer leaves the drawer entirely (Send is disabled, so focus goes to body → skip link → nav). On mobile the drawer is full-screen with body scroll locked — modal behaviour without modal semantics.
- [P1] `#ask-drawer-input` has NO visible focus indicator; `outline-style: none`, no box-shadow, no border change. Three different focus treatments across the drawer's seven controls.
- [P1] 32 standalone controls under 24×24 at 390px, all 19px tall — tag links, "Read everything →", "About the author →", the contents `<summary>`, home suggested questions. The touch-target fix was applied per component, not per pattern; `sm:min-h-0` also restores sub-44px targets at 640px, covering tablets in portrait.
- [P1] `/professional` primary CTA fails contrast at 3.46:1 — near-black on Moss (`bg-secondary`). A conversion control, and cycle 3 already flagged Moss-as-CTA.
- [P1] `/about` has zero h1. `/projects` skips h1→h3 — introduced by this cycle's thesis restructure, which made the card title an h3 with no h2 above it.
- [P2] The width fix missed the chrome: `.container` in `navbar.tsx:24` and `footer-section.tsx:26` resolves to 1280px, 128px wider than every content column beneath it. Roughly 25 routes also nest a hand-rolled max-w inside Container, two of them (3xl, 5xl) off the scale entirely.
- [P2] `POST /api/views` returns 403 on both essay routes at both widths, and 429 under repeat load.
- [P2] The album blurbs assert unsourced facts. The commit shipping them documents that they fail PRODUCT.md's binding constraint. The repo has begun recording its violations instead of enforcing them.

## Regressions this cycle introduced

1. The drawer mounts `ChatBubble`/`ChatMessageList` unchanged: rounded-2xl bubbles, `bg-primary` fills on every user turn (Forest Ink as area, repeatedly), and `shadow-md`. A foreign idiom, now reachable from every route.
2. A `Sparkle` icon is now the site's most persistent mark, in the fixed header on every route. DESIGN.md names SaaS chrome as one of three things the system deliberately is not.
3. The drawer has two nested scroll containers and an effective measure of ~280px (~40 characters) after avatar and padding.
4. The essay's left rail leaves 172px of dead paper on the right of every post; 260px is also below DESIGN.md's stated 280–360px rail range.
5. `blog-sidebar.tsx:102` carries `hover:border-gray-300 dark:hover:border-gray-700` into a file this cycle touched. `scrollToHeading` hardcodes smooth behaviour with no reduced-motion check.
6. `/movies` and `/books` kept the 4-up stat-tile grid that was deleted from /blog and /projects in the same cycle. Removal was applied by route, not by pattern.
7. DESIGN.md:250 is now stale: it lists `blog-stats.tsx` as carrying an inset shadow, and that file no longer exists.

## Minor

- The new essay is staged `seedling` but carries named evidence and original synthesis — that is at least budding. It also uses `default.webp` where every other post has a bespoke cover, and runs four negative parallelisms where writing-style permits one.
- `navbar.tsx:15` `ACCENT_HREF = ""` still makes the entire accent branch unreachable.
- `currently-strip.tsx:102` truncates a title by 149px.
- `audio-player-controls.tsx` is a second palette (stone-*) with a second primary fill, above the prose on all 83 essays, unopened across four cycles.
- The CSP allowlist for the live detector is `NODE_ENV === "development"`-gated, so a production build cannot be inspected the documented way.

## Questions

- What is a binding constraint you can satisfy by writing down that you violated it?
- The drawer exists because the essay sidebar navigated readers away. The masthead ask field still does exactly that. Why did the fix land on the smaller surface?
- Four cycles have found and fixed smaller things while a second design system sat above the prose on all 83 essays. Is the review finding what is easy to grep rather than what readers see?
